# This file ensures the public/uploads/ directory exists in the git repo.
# Decap CMS commits uploads here at runtime; the directory must exist before
# the first commit. Safe to delete after the first upload happens.
